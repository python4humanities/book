letters = ['a', 'b', 'c', 'd', 'e']
letters_enum = enumerate(letters)      # 이터레이터인 enumerate객체 생성
print(letters_enum)                    # enumerate 객체 출력
print(list(letters_enum))              # enumerate 객체를 list로 형변환해 출력
